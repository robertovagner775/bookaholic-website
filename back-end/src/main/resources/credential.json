{
    "client_id" : "${CLIENT_ID}",
    "client_secret" : "${CLIENT_SECRET}",
    "certificate" : "./certs/producao-516961-bookaholic_pro.p12",
    "sandbox" : "${SANDBOX}",
    "debug" : "${DEBUG}"

}